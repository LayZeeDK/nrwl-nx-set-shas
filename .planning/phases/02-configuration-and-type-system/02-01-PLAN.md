---
phase: 02-configuration-and-type-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - action.yml
  - package.json
  - tsconfig.json
  - .github/workflows/test.yml
  - .github/workflows/test-integration.yml
  - .github/workflows/format.yml
  - .github/workflows/publish.yml
  - .github/workflows/integration-test-workflow.yml
autonomous: true
requirements:
  - RUNT-01
  - RUNT-02
  - RUNT-03
  - TSCO-01
  - TSCO-02

must_haves:
  truths:
    - 'action.yml declares node24 as the runtime'
    - 'package.json Volta pin is 24.14.0 and engines.node is >=24'
    - 'tsconfig.json target and lib are both ES2024'
    - 'FORCE_JAVASCRIPT_ACTIONS_TO_NODE24 flag is removed from all 5 CI workflows'
  artifacts:
    - path: 'action.yml'
      provides: 'Node.js 24 runtime declaration'
      contains: "using: 'node24'"
    - path: 'package.json'
      provides: 'Node.js 24 toolchain configuration'
      contains: '24.14.0'
    - path: 'tsconfig.json'
      provides: 'ES2024 TypeScript compiler settings'
      contains: 'ES2024'
  key_links:
    - from: 'action.yml'
      to: 'GitHub Actions runner'
      via: 'runs.using field'
      pattern: "using: 'node24'"
    - from: 'package.json'
      to: 'Volta'
      via: 'volta.node field'
      pattern: "24\\.14\\.0"
---

<objective>
Update all runtime and toolchain configuration to target Node.js 24.

Purpose: With action.yml declaring node24, GitHub Actions runners will natively use Node.js 24 -- the FORCE flag becomes redundant and is removed. TypeScript compiler settings are bumped to ES2024 per the official Node Target Mapping.
Output: All configuration files point to Node.js 24 / ES2024.
</objective>

<execution_context>
@C:/Users/LarsGyrupBrinkNielse/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/LarsGyrupBrinkNielse/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-configuration-and-type-system/02-CONTEXT.md
@.planning/phases/02-configuration-and-type-system/02-RESEARCH.md

<interfaces>
<!-- Configuration files to modify. All are small, self-contained config. -->

From action.yml (line 44-46):

```yaml
runs:
  using: 'node20'
  main: 'dist/nx-set-shas.js'
```

From package.json (lines 16-21, 29):

```json
{
  "engines": { "node": ">=20" },
  "volta": { "node": "20.19.4" },
  "devDependencies": { "@types/node": "^20.19.9" }
}
```

From tsconfig.json (full file):

```json
{
  "compilerOptions": {
    "lib": ["ES2023"],
    "module": "nodenext",
    "target": "ES2023"
  }
}
```

All 5 workflow files have this top-level env block (only variable):

```yaml
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update runtime declaration and toolchain config</name>
  <files>action.yml, package.json, tsconfig.json</files>
  <action>
Edit action.yml line 45: change `using: 'node20'` to `using: 'node24'`.

Edit package.json:

- Line 17: change `"node": ">=20"` to `"node": ">=24"` (engines field)
- Line 20: change `"node": "20.19.4"` to `"node": "24.14.0"` (volta pin)
- Do NOT change the `@types/node` version in this task (that is Plan 02)
- Do NOT change `packageManager`, `version`, or any other field

Edit tsconfig.json:

- Change `"lib": ["ES2023"]` to `"lib": ["ES2024"]`
- Change `"target": "ES2023"` to `"target": "ES2024"`
- Do NOT change `"module": "nodenext"` (locked decision: module stays unchanged)
  </action>
  <verify>
  <automated>node -e "const a=require('js-yaml').load(require('fs').readFileSync('action.yml','utf8')); console.assert(a.runs.using==='node24','action.yml'); const p=require('./package.json'); console.assert(p.engines.node==='>=24','engines'); console.assert(p.volta.node==='24.14.0','volta'); const t=require('./tsconfig.json'); console.assert(t.compilerOptions.target==='ES2024','target'); console.assert(t.compilerOptions.lib[0]==='ES2024','lib'); console.log('[OK] All config values correct')"</automated>
  </verify>
  <done>action.yml declares node24, package.json has engines >=24 and volta 24.14.0, tsconfig.json has ES2024 target and lib</done>
  </task>

<task type="auto">
  <name>Task 2: Remove FORCE_JAVASCRIPT_ACTIONS_TO_NODE24 from all CI workflows</name>
  <files>.github/workflows/test.yml, .github/workflows/test-integration.yml, .github/workflows/format.yml, .github/workflows/publish.yml, .github/workflows/integration-test-workflow.yml</files>
  <action>
Remove the entire top-level `env:` block from each of the 5 workflow files listed below. In every case, the env block contains only `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true` and no other variables, so the entire block (both the `env:` key line and the variable line) must be removed. Also remove any resulting blank lines that would leave two consecutive blank lines.

Files:

1. `.github/workflows/test.yml` (lines 11-12)
2. `.github/workflows/test-integration.yml` (lines 11-12)
3. `.github/workflows/format.yml` (lines 8-9)
4. `.github/workflows/publish.yml` (lines 7-8)
5. `.github/workflows/integration-test-workflow.yml` (lines 18-19)

The FORCE flag was added in Phase 1 (commit 1cc440b) as a temporary testing bridge. Now that action.yml declares node24 natively, the flag is redundant.

Do NOT modify any other content in these workflow files.
</action>
<verify>
<automated>git grep -c "FORCE_JAVASCRIPT_ACTIONS_TO_NODE24" -- ".github/workflows/\*.yml" && echo "[ERROR] FORCE flag still present" && exit 1 || echo "[OK] FORCE flag removed from all workflows"</automated>
</verify>
<done>Zero occurrences of FORCE_JAVASCRIPT_ACTIONS_TO_NODE24 in any workflow file</done>
</task>

</tasks>

<verification>
All 5 requirements verified:
- RUNT-01: `git grep "using: 'node24'" -- action.yml` returns a match
- RUNT-02: `node -e "console.assert(require('./package.json').volta.node==='24.14.0')"`
- RUNT-03: `node -e "console.assert(require('./package.json').engines.node==='>=24')"`
- TSCO-01: `node -e "console.assert(require('./tsconfig.json').compilerOptions.target==='ES2024')"`
- TSCO-02: `node -e "console.assert(require('./tsconfig.json').compilerOptions.lib[0]==='ES2024')"`
</verification>

<success_criteria>

1. action.yml line 45 reads `using: 'node24'`
2. package.json engines.node is `>=24`, volta.node is `24.14.0`
3. tsconfig.json target and lib are both `ES2024`, module is still `nodenext`
4. No occurrence of `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24` in any .github/workflows/\*.yml file
   </success_criteria>

<output>
After completion, create `.planning/phases/02-configuration-and-type-system/02-01-SUMMARY.md`
</output>
