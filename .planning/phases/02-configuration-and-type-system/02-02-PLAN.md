---
phase: 02-configuration-and-type-system
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - package.json
  - bun.lock
autonomous: true
requirements:
  - TYPE-01
  - TYPE-02

must_haves:
  truths:
    - '@types/node is at 24.x in package.json'
    - 'tsc --noEmit produces no new errors beyond the pre-existing TS2307'
    - 'bun.lock is updated to reflect the @types/node version change'
  artifacts:
    - path: 'package.json'
      provides: '@types/node 24.x dependency'
      contains: '^24'
    - path: 'bun.lock'
      provides: 'Updated lockfile with @types/node 24.x resolved'
  key_links:
    - from: 'package.json'
      to: 'bun.lock'
      via: 'bun install'
      pattern: '@types/node.*24'
    - from: '@types/node@24'
      to: 'nx-set-shas.ts'
      via: 'TypeScript type checking'
      pattern: 'tsc --noEmit'
---

<objective>
Update @types/node to 24.x and verify TypeScript compilation produces no new errors.

Purpose: With runtime and compiler config pointing to Node.js 24 / ES2024 (from Plan 01), the type definitions must match. This is the highest-risk change in Phase 2 -- if Node.js 24 introduced breaking type changes, they surface here.
Output: @types/node 24.x installed, lockfile updated, tsc --noEmit confirms no new errors.
</objective>

<execution_context>
@C:/Users/LarsGyrupBrinkNielse/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/LarsGyrupBrinkNielse/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-configuration-and-type-system/02-CONTEXT.md
@.planning/phases/02-configuration-and-type-system/02-RESEARCH.md
@.planning/phases/02-configuration-and-type-system/02-01-SUMMARY.md

<interfaces>
<!-- From package.json (line 29) -->
```json
{
  "devDependencies": {
    "@types/node": "^20.19.9"
  }
}
```

<!-- Pre-existing tsc error (NOT caused by this migration, do NOT fix): -->
<!-- TS2307: Cannot find module '@actions/github/lib/utils' -->
<!-- This error exists today with @types/node@20 and is out of scope. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump @types/node to 24.x and update lockfile</name>
  <files>package.json, bun.lock</files>
  <action>
Edit package.json line 29: change `"@types/node": "^20.19.9"` to `"@types/node": "^24.12.0"`.

Then run `bun install` to update bun.lock with the resolved @types/node 24.x package.

Do NOT change any other dependency versions.
</action>
<verify>
<automated>node -e "const p=require('./package.json'); const v=p.devDependencies['@types/node']; console.assert(v.startsWith('^24'), 'Expected ^24.x, got ' + v); console.log('[OK] @types/node is ' + v)"</automated>
</verify>
<done>package.json has @types/node ^24.12.0 and bun.lock is updated</done>
</task>

<task type="auto">
  <name>Task 2: Verify TypeScript compilation -- no new errors</name>
  <files></files>
  <action>
Run `npx tsc --noEmit` and capture the output.

Expected outcome: The ONLY error should be the pre-existing `TS2307: Cannot find module '@actions/github/lib/utils'` error. This error exists today with @types/node@20 and is NOT caused by the Node.js 24 migration.

If any NEW errors appear (errors that were not present before the @types/node bump):

- Fix them inline in the source files (nx-set-shas.ts or tools/pre-commit.ts)
- Common Node.js 24 type changes to watch for: `catch (e)` clause typing changes, removed/renamed API types
- Document what was fixed and why

If no new errors appear (only the pre-existing TS2307), the task is complete with no source changes needed.

Do NOT attempt to fix the pre-existing TS2307 error -- it is out of scope for this migration.
</action>
<verify>
<automated>npx tsc --noEmit 2>&1 | tee /tmp/tsc-output.txt; NEW_ERRORS=$(cat /tmp/tsc-output.txt | rg -v "TS2307.*@actions/github/lib/utils" | rg "error TS" | wc -l); if [ "$NEW_ERRORS" -gt 0 ]; then echo "[ERROR] $NEW_ERRORS new TypeScript errors found"; cat /tmp/tsc-output.txt | rg "error TS" | rg -v "TS2307.\*@actions/github/lib/utils"; exit 1; else echo "[OK] No new TypeScript errors (pre-existing TS2307 is expected)"; fi</automated>
</verify>
<done>tsc --noEmit produces zero new errors beyond the pre-existing TS2307. If new errors appeared, they are fixed and documented.</done>
</task>

</tasks>

<verification>
Both requirements verified:
- TYPE-01: `node -e "console.assert(require('./package.json').devDependencies['@types/node'].startsWith('^24'))"`
- TYPE-02: `npx tsc --noEmit` shows only the pre-existing TS2307 error, no new errors from @types/node 24.x
</verification>

<success_criteria>

1. package.json devDependencies has @types/node ^24.12.0
2. bun.lock reflects the updated @types/node resolution
3. `tsc --noEmit` produces no new errors (only pre-existing TS2307 is acceptable)
   </success_criteria>

<output>
After completion, create `.planning/phases/02-configuration-and-type-system/02-02-SUMMARY.md`
</output>
