---
phase: 03-source-fixes-and-build
plan: 02
type: execute
wave: 2
depends_on:
  - '03-01'
files_modified:
  - dist/nx-set-shas.js
autonomous: true
requirements:
  - BVAL-01

must_haves:
  truths:
    - 'dist/nx-set-shas.js is rebuilt with the upgraded @actions/* ESM dependencies'
    - 'The built artifact reflects all source and dependency changes from Phase 2 and 3'
    - 'CI passes on all 3 OS matrix runners (ubuntu, macos, windows)'
  artifacts:
    - path: 'dist/nx-set-shas.js'
      provides: 'Bundled action runtime artifact'
      min_lines: 10
  key_links:
    - from: 'nx-set-shas.ts'
      to: 'dist/nx-set-shas.js'
      via: 'bun build ./nx-set-shas.ts --outdir ./dist --target node'
      pattern: '@actions/core'
    - from: 'dist/nx-set-shas.js'
      to: 'action.yml'
      via: 'action.yml main field references dist/nx-set-shas.js'
      pattern: 'dist/nx-set-shas'
---

<objective>
Rebuild the dist/ artifact with the upgraded dependencies and verify CI passes on all platforms.

Purpose: Produce a fresh `dist/nx-set-shas.js` that bundles the new ESM-only @actions/\* 3.x/9.x dependencies, then push and confirm CI green on all 3 OS runners. This is the final gate before Phase 4 (version bump and PR).
Output: Updated dist/nx-set-shas.js committed, CI green.
</objective>

<execution_context>
@C:/Users/LarsGyrupBrinkNielse/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/LarsGyrupBrinkNielse/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-source-fixes-and-build/03-CONTEXT.md
@.planning/phases/03-source-fixes-and-build/03-RESEARCH.md
@.planning/phases/03-source-fixes-and-build/03-01-SUMMARY.md

@package.json
@nx-set-shas.ts
@action.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild dist/ and verify CI green</name>
  <files>dist/nx-set-shas.js</files>
  <action>
Execute the build validation sequence defined in CONTEXT.md:

1. Run `bun run build` to produce a fresh `dist/nx-set-shas.js` bundling the ESM-only @actions/\* 3.x/9.x deps
2. Run `npx tsc --noEmit` to confirm type checking still passes
3. Review `git diff --stat dist/` to see what changed in the built artifact (expect significant changes since the bundled deps went from CJS to ESM and jumped major versions)
4. Commit: `build(03): rebuild dist/ for node24 runtime`
5. Push to remote
6. Verify CI passes using `gh run watch` or equivalent -- all 3 OS matrix runners (ubuntu, macos, windows) must pass

Note: The pre-commit hook runs `npm run build` which delegates to `bun build` and auto-stages dist/ changes. This is expected behavior -- the hook will fire during the commit and may re-stage dist/. This is fine since the dist/ changes are exactly what we want to commit.

If CI fails, diagnose and fix within this plan. Phase 3 owns CI green per CONTEXT.md decision. Common failure modes:

- Node.js version mismatch: action.yml already declares node24, fork CI uses FORCE flag
- Octokit response shape changes: unlikely since we use raw `octokit.request()` with string templates
- ESM resolution issues in bundled output: check Bun bundler output for import errors
  </action>
  <verify>
  <automated>bun run build &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <done>dist/nx-set-shas.js is rebuilt and committed, pushed to remote, CI passes on all 3 OS matrix runners</done>
  </task>

</tasks>

<verification>
- `dist/nx-set-shas.js` exists and is freshly built with ESM @actions/* deps
- `bun run build` exits with code 0
- `npx tsc --noEmit` exits with code 0
- Git log shows the 3 commits in order: audit doc, dep bump, dist rebuild
- CI workflows (test, integration-test, format) pass on all runners
</verification>

<success_criteria>

- dist/nx-set-shas.js rebuilt with Node.js 24 compatible dependencies (BVAL-01)
- CI green on all platforms
- Phase 3 complete and ready for Phase 4
  </success_criteria>

<output>
After completion, create `.planning/phases/03-source-fixes-and-build/03-02-SUMMARY.md`
</output>
