---
phase: 03-source-fixes-and-build
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/03-source-fixes-and-build/03-AUDIT.md
  - package.json
  - bun.lock
autonomous: true
requirements:
  - AUDT-01
  - AUDT-02
  - AUDT-03

must_haves:
  truths:
    - 'All Node.js built-in API usage in nx-set-shas.ts is audited with Node.js 24 compatibility status'
    - 'All Node.js built-in API usage in tools/pre-commit.ts is audited with Node.js 24 compatibility status'
    - 'Audit findings are documented in a structured checklist table'
    - '@actions/core is upgraded to 3.x and @actions/github is upgraded to 9.x'
    - 'tsc --noEmit passes after the dependency upgrade'
  artifacts:
    - path: '.planning/phases/03-source-fixes-and-build/03-AUDIT.md'
      provides: 'Node.js 24 API compatibility audit document'
      contains: 'spawnSync'
    - path: 'package.json'
      provides: 'Updated @actions/* dependency versions'
      contains: '@actions/core'
    - path: 'bun.lock'
      provides: 'Updated lockfile with new dependency tree'
  key_links:
    - from: 'package.json'
      to: 'bun.lock'
      via: 'bun add resolves and locks transitive deps'
      pattern: "@actions/core.*3\\."
---

<objective>
Audit all Node.js API usage for Node.js 24 compatibility and upgrade @actions/* dependencies to ESM-only versions with official Node 24 support.

Purpose: Document that all Node.js built-in APIs used are compatible with Node.js 24 (zero breaking changes), then upgrade @actions/core to 3.x and @actions/github to 9.x to gain official Node 24 support and fix the TS2307 error on `@actions/github/lib/utils`.
Output: 03-AUDIT.md document, updated package.json and bun.lock with new @actions/\* versions.
</objective>

<execution_context>
@C:/Users/LarsGyrupBrinkNielse/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/LarsGyrupBrinkNielse/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-source-fixes-and-build/03-CONTEXT.md
@.planning/phases/03-source-fixes-and-build/03-RESEARCH.md

@nx-set-shas.ts
@tools/pre-commit.ts
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Node.js 24 API compatibility audit document</name>
  <files>.planning/phases/03-source-fixes-and-build/03-AUDIT.md</files>
  <action>
Create `.planning/phases/03-source-fixes-and-build/03-AUDIT.md` with an API-by-API checklist table covering every Node.js built-in API used in the two source files.

For `nx-set-shas.ts`, audit these APIs with file and line numbers:

- `spawnSync` (import line 4; usage lines 40, 52, 63, 98, 103, 250) -- stable, no breaking changes
- `existsSync` (import line 5; usage line 30) -- stable, Node.js 24 adds runtime warning for invalid inputs but this codebase passes valid string paths only
- `process.chdir` (line 31) -- stable
- `process.stdout.write` (lines 33-34, 87-89, 111-113, 115-117, 122-124, 131-132, 143-149, 155-157) -- stable
- `process.env` (line 12, line 185) -- stable

For `tools/pre-commit.ts`, audit these APIs:

- `execSync` (import line 1; usage lines 13, 18, 26, 34) -- stable, no breaking changes
- `process.exit` (lines 7, 41) -- stable
- `process.env` (lines 15, 21) -- stable

Table columns: API, File, Line(s), Node.js 24 Status, Action Needed.

Include sections for:

1. Summary (zero breaking changes found)
2. Audit table for nx-set-shas.ts
3. Audit table for tools/pre-commit.ts
4. Findings: note the pre-commit hook `npm run build` vs `bun run build` discrepancy (do NOT fix it)
5. Out-of-scope items: `/dev/null` git argument (line 105), `stripNewLineEndings()` latent bug (SRCF-01), stdio normalization (SRCF-02)

Commit separately: `docs(03): audit Node.js 24 API compatibility`
</action>
<verify>
<automated>test -f .planning/phases/03-source-fixes-and-build/03-AUDIT.md &amp;&amp; git grep -c "spawnSync" .planning/phases/03-source-fixes-and-build/03-AUDIT.md</automated>
</verify>
<done>03-AUDIT.md exists with complete API-by-API checklist tables for both source files, committed to git</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade @actions/core to 3.x and @actions/github to 9.x</name>
  <files>package.json, bun.lock</files>
  <action>
Run the dependency upgrade:

```bash
bun add @actions/core@^3.0.0 @actions/github@^9.0.0
```

After the upgrade, verify:

1. `npx tsc --noEmit` passes with zero errors (the TS2307 error on `@actions/github/lib/utils` should be resolved since @actions/github 9.0.0 explicitly exports `./lib/utils` in its exports map)
2. package.json shows `@actions/core` at `^3.0.0` and `@actions/github` at `^9.0.0`
3. bun.lock is updated with the new dependency tree

If `tsc --noEmit` surfaces unexpected errors after the upgrade (e.g., `catch (e)` typing from stricter @actions/core types), fix them with type guards or assertions. Per research, strict mode is NOT enabled in tsconfig so `catch (e)` should remain typed as `any`.

Do NOT modify the import at line 3 of nx-set-shas.ts (`import { GitHub } from '@actions/github/lib/utils'`) -- it is a valid import with @actions/github 9.0.0.

Do NOT add `--format=cjs` to any build command -- the project is ESM.

Commit: `feat(03): bump @actions/core to 3.x and @actions/github to 9.x`
</action>
<verify>
<automated>npx tsc --noEmit</automated>
</verify>
<done>package.json has @actions/core ^3.0.0 and @actions/github ^9.0.0, bun.lock updated, tsc --noEmit passes with zero errors, committed to git</done>
</task>

</tasks>

<verification>
- 03-AUDIT.md exists and documents all Node.js built-in APIs from both source files
- package.json dependencies show @actions/core ^3.0.0 and @actions/github ^9.0.0
- `npx tsc --noEmit` exits with code 0
- Two separate commits exist: audit doc first, then dependency bump
</verification>

<success_criteria>

- All Node.js API usage audited and documented (AUDT-01, AUDT-02, AUDT-03)
- @actions/\* dependencies upgraded to ESM-only versions with Node 24 support
- TypeScript compilation passes cleanly
  </success_criteria>

<output>
After completion, create `.planning/phases/03-source-fixes-and-build/03-01-SUMMARY.md`
</output>
