---
phase: 01-baseline-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/test.yml
  - .github/workflows/integration-test-workflow.yml
  - .github/workflows/test-integration.yml
  - .github/workflows/format.yml
  - .github/workflows/publish.yml
  - .planning/phases/01-baseline-validation/01-BASELINE-RESULTS.md
autonomous: false
requirements:
  - BVAL-02
  - BVAL-03

must_haves:
  truths:
    - 'All workflow files use actions/checkout@v6 instead of v4'
    - 'All workflow files have FORCE_JAVASCRIPT_ACTIONS_TO_NODE24=true at workflow-level env'
    - 'CI runs complete on all three platforms (ubuntu, macOS, Windows) for test and integration-test workflows'
    - 'Baseline pass/fail results are recorded with CI run links and dependency versions'
  artifacts:
    - path: '.github/workflows/test.yml'
      provides: 'Test workflow with node24 FORCE flag and checkout v6'
      contains: 'FORCE_JAVASCRIPT_ACTIONS_TO_NODE24'
    - path: '.github/workflows/integration-test-workflow.yml'
      provides: 'Integration test reusable workflow with node24 FORCE flag and checkout v6'
      contains: 'FORCE_JAVASCRIPT_ACTIONS_TO_NODE24'
    - path: '.github/workflows/test-integration.yml'
      provides: 'Integration test caller with node24 FORCE flag'
      contains: 'FORCE_JAVASCRIPT_ACTIONS_TO_NODE24'
    - path: '.github/workflows/format.yml'
      provides: 'Format check workflow with node24 FORCE flag and checkout v6'
      contains: 'FORCE_JAVASCRIPT_ACTIONS_TO_NODE24'
    - path: '.github/workflows/publish.yml'
      provides: 'Publish workflow with node24 FORCE flag and checkout v6'
      contains: 'FORCE_JAVASCRIPT_ACTIONS_TO_NODE24'
    - path: '.planning/phases/01-baseline-validation/01-BASELINE-RESULTS.md'
      provides: 'Baseline validation results matrix'
      min_lines: 20
  key_links:
    - from: '.github/workflows/test-integration.yml'
      to: '.github/workflows/integration-test-workflow.yml'
      via: 'reusable workflow call'
      pattern: 'uses:.*integration-test-workflow'
    - from: '.github/workflows/integration-test-workflow.yml'
      to: 'FORCE_JAVASCRIPT_ACTIONS_TO_NODE24'
      via: 'workflow-level env (NOT inherited from caller)'
      pattern: 'FORCE_JAVASCRIPT_ACTIONS_TO_NODE24'
---

<objective>
Bump all CI workflow action dependencies to their latest node24-native major versions, add the `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24=true` environment variable to every workflow file, push to trigger CI, and record baseline pass/fail results.

Purpose: Establish evidence-based baseline of whether the action works on Node.js 24 before making any source code changes. This scopes how much fixing Phases 2-3 actually need.
Output: Updated workflow files (two commits), CI run results, and `01-BASELINE-RESULTS.md` documenting the full workflow x platform matrix.
</objective>

<execution_context>
@C:/Users/LarsGyrupBrinkNielse/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/LarsGyrupBrinkNielse/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-baseline-validation/01-CONTEXT.md
@.planning/phases/01-baseline-validation/01-RESEARCH.md

Source files to modify:
@.github/workflows/test.yml
@.github/workflows/integration-test-workflow.yml
@.github/workflows/test-integration.yml
@.github/workflows/format.yml
@.github/workflows/publish.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump action dependencies and add FORCE_JAVASCRIPT_ACTIONS_TO_NODE24 flag</name>
  <files>.github/workflows/test.yml, .github/workflows/integration-test-workflow.yml, .github/workflows/test-integration.yml, .github/workflows/format.yml, .github/workflows/publish.yml</files>
  <action>
This task produces TWO separate commits per user decision. Dep bumps first, then FORCE flag.

**Commit 1: Bump action dependencies to node24-native versions**

In ALL five workflow files, update `actions/checkout` from `v4` to `v6`:

- `test.yml`: Two `uses: actions/checkout@v4` references (lines 24, 32) -> `actions/checkout@v6`
- `integration-test-workflow.yml`: Two `uses: actions/checkout@v4` references (lines 26, 36) -> `actions/checkout@v6`
- `format.yml`: One `uses: actions/checkout@v4` reference (line 13) -> `actions/checkout@v6`
- `publish.yml`: One `uses: actions/checkout@v4` reference (line 11) -> `actions/checkout@v6`
- `test-integration.yml`: No checkout references (only calls reusable workflow). No changes needed in this file for commit 1.

`oven-sh/setup-bun@v2` stays at v2 (already node24-native per research). `jameshenry/publish-shell-action@v1` stays at v1 (no newer version exists; note for upstream PR).

No `with:` input changes needed -- `ref`, `fetch-depth`, `filter` inputs are preserved across v4/v5/v6. No breaking changes affect this repo's usage patterns.

Commit message: `chore(ci): bump action dependencies to node24-native versions`
Stage ONLY the four modified workflow files (not test-integration.yml since it has no changes in this commit).

**Commit 2: Add FORCE_JAVASCRIPT_ACTIONS_TO_NODE24 flag**

In ALL five workflow files, add a workflow-level `env:` block with `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true`. The `env:` block goes at the top level of the YAML, after the `on:` trigger block and before `concurrency:` (if present) or `jobs:`.

Placement per file:

- `test.yml`: Add `env:` block between the `pull_request:` trigger block and `concurrency:`, so the final order is: `name:`, `on:`, `env:`, `concurrency:`, `jobs:`
- `integration-test-workflow.yml`: Add `env:` block between `on:` block and `jobs:`. CRITICAL: This file MUST have the flag set directly -- it does NOT inherit env vars from the caller `test-integration.yml` because reusable workflows run in an isolated context.
- `test-integration.yml`: Add `env:` block between `on:` block and `concurrency:`. Even though this workflow only calls a reusable workflow and has no direct action steps, add the flag for completeness per user decision.
- `format.yml`: Add `env:` block between `on:` block and `jobs:`.
- `publish.yml`: Add `env:` block between `on:` block and `jobs:`.

Format for the env block:

```yaml
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
```

Commit message: `test: [TEMP] add FORCE_JAVASCRIPT_ACTIONS_TO_NODE24 for baseline validation`
Stage all five workflow files.
</action>
<verify>
<automated>git log --oneline -2 && git grep "FORCE_JAVASCRIPT_ACTIONS_TO_NODE24" -- ".github/workflows/" | wc -l && git grep "actions/checkout@v6" -- ".github/workflows/" | wc -l</automated>
Expect: 2 commits visible, 5 files containing FORCE flag, 6 references to actions/checkout@v6 (two in test.yml, two in integration-test-workflow.yml, one in format.yml, one in publish.yml).
</verify>
<done>All five workflow files use actions/checkout@v6. All five workflow files have FORCE_JAVASCRIPT_ACTIONS_TO_NODE24=true at workflow-level env. Two separate commits exist with correct messages.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Push branch, verify CI results, and record baseline</name>
  <files>.planning/phases/01-baseline-validation/01-BASELINE-RESULTS.md</files>
  <action>
Push the branch to trigger CI, monitor all workflow runs, and record results.

1. Push branch: `git push`
2. Monitor CI with `gh run list --branch $(git branch --show-current) --limit 10` until all three workflows (Test, Test Integration, Check Formatting) complete.
3. Collect results with `gh run view <run-id>` for each workflow run:
   - Pass/fail status per workflow per platform (ubuntu, macOS, Windows)
   - Error messages from any failed jobs
   - Whether the action produced correct NX_BASE and NX_HEAD values
4. Verify Node.js 24 was actually used: check runner logs for the action execution step. When FORCE is active, the runner uses the node24 binary. NOTE: Deprecation annotations saying "running on Node.js 20" should be IGNORED -- they fire based on the declared version in action.yml, not the actual runtime (known runner bug actions/runner#4295).
5. Create `.planning/phases/01-baseline-validation/01-BASELINE-RESULTS.md` containing:
   - Workflow x platform matrix table (pass/fail per combination)
   - CI run link(s) (GitHub Actions URL from `gh run view --json url`)
   - Failure details (error messages, log excerpts) if anything fails
   - Dependency versions used (actions/checkout@v6, oven-sh/setup-bun@v2, jameshenry/publish-shell-action@v1)
   - Summary conclusion informing Phase 2-3 scope
6. Commit the results file.

**Checkpoint:** After results are recorded, user confirms CI results are acceptable.
</action>
<verify>
<automated>test -f .planning/phases/01-baseline-validation/01-BASELINE-RESULTS.md && echo "Results file exists"</automated>
</verify>
<done>01-BASELINE-RESULTS.md exists with workflow x platform matrix, CI links, dependency versions, and scope conclusion. User has confirmed results.</done>
</task>

</tasks>

<verification>
- All five workflow files contain `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true` at workflow-level env
- All workflow files using `actions/checkout` reference `@v6` (not `@v4`)
- `oven-sh/setup-bun` remains at `@v2` (already node24-native)
- `jameshenry/publish-shell-action` remains at `@v1` (no newer version)
- Two commits exist: dep bumps first, FORCE flag second
- FORCE flag commit message matches required format: `test: [TEMP] add FORCE_JAVASCRIPT_ACTIONS_TO_NODE24 for baseline validation`
- CI has run on all platforms and results are recorded in `01-BASELINE-RESULTS.md`
</verification>

<success_criteria>

1. CI test workflow runs with `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24=true` and results are recorded
2. Baseline pass/fail status is documented so Phase 2-3 scope is informed by evidence, not assumptions
3. `01-BASELINE-RESULTS.md` exists with workflow x platform matrix, CI links, dependency versions, and scope conclusion
   </success_criteria>

<output>
After completion, create `.planning/phases/01-baseline-validation/01-01-SUMMARY.md`
</output>
